name: Hello World Container

on:
  workflow_dispatch:
  push:

env:
  ACTIONS_STEP_DEBUG: true

jobs:
  hello:
    runs-on: self-hosted
    container:
      image: node:24
    steps:
      - run: | 
          echo starting sleep
          sleep 180
          echo "never see me!"
  # fail:
  #   runs-on: self-hosted
  #   container:
  #     image: node:24
  #   steps:
  #     - run: exit 1
      
